"use client";

import React, { useState } from 'react';
import "../footer.css";
import { Divider } from '@mui/material'

export default function Footer() {
    const [isPopupVisible, setIsPopupVisible] = useState(false);

    const togglePopup = () => {
        setIsPopupVisible(!isPopupVisible);
    }

    return (
        <footer>
            <div className="footer-container">
                <button 
                    id="back-to-top" 
                    onClick={() => window.scrollTo({ top: 0, behavior: 'smooth' })}
                >
                    BACK TO TOP
                </button>
                <button className="popup" onClick={togglePopup}>
                    FAQ
                    <button/>
                    <span className={`popuptext ${isPopupVisible ? "show" : ""}`} id="myPopup">
                        <ul>
                            <li className='mb-1'><strong>Is this a paid website?</strong></li>
                            <li>It is free to use, but some of the features will be paid. Payment is required to access all features</li>
                            <Divider
                                sx={{
                                    my: 1,
                                    height: 2,
                                    background: 'linear-gradient(to right, #bfdf3c, #2ee37e, #bfdf3c)',
                                    borderRadius: 1,
                                }}
                                />
                            <li className='mb-1'><strong>Are the cards generated by AI?</strong></li>
                            <li>Yes, you can search any topic</li>
                            <Divider
                                sx={{
                                    my: 1,
                                    height: 2,
                                    background: 'linear-gradient(to right, #bfdf3c, #2ee37e, #bfdf3c)',
                                    borderRadius: 1,
                                }}
                                />
                            <li className='mb-1'><strong>Can I customize my own cards?</strong></li>
                            <li>Yes, you can add, edit, & delete the flashcards</li>
                            <Divider
                                sx={{
                                    my: 1,
                                    height: 2,
                                    background: 'linear-gradient(to right, #bfdf3c, #2ee37e, #bfdf3c)',
                                    borderRadius: 1,
                                }}
                                />
                            <li className='mb-1'><strong>Is my search protected?</strong></li>
                            <li>Yes, all data is private</li>
                        </ul>
                    </span>
                </button>
                <div className="footer-bottom">
                    <p>&copy; 2024 INTEL-O-FLASH</p>
                </div>
            </div>
        </footer>
    )
}